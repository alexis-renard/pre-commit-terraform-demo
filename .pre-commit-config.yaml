# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-yaml
      - id: trailing-whitespace
      - id: end-of-file-fixer

  - repo: https://github.com/antonbabenko/pre-commit-terraform
    rev: v1.100.0
    hooks:
      - id: terraform_fmt
      - id: terraform_tflint
        args:
          - 'args=--deep'
      # - id: terraform_tfsec # removed because of its incompatibility with tf > 1.5. Now using trivy instead.
      # - id: terraform_trivy
      #   args:
      #     # Note : all of this didn't manage to skip the other-code-example folder. The issue https://github.com/antonbabenko/pre-commit-terraform/issues/617 might still be relevant and should be re-open.
      #     - --args=--skip-files="**/other-code-examples/**/*"
      #     - --args=--skip-files="**/other-code-examples/**/*"
      #     - --args=--skip-dirs="**/other-code-examples"
      #     - --args=--skip-dirs="**/ec2"
      # - id: terraform_docs

  # - repo: https://github.com/Yelp/detect-secrets
  #   rev: v1.5.0
  #   hooks:
  #     - id: detect-secrets
  #       args: ['--baseline', '.pre-commit-config/.secrets.baseline']
  #       exclude: .*/tests/.*

  # - repo: https://github.com/frnmst/md-toc
  #   rev: 9.0.0
  #   hooks:
  #     - id: md-toc
  #       language_version: python3
